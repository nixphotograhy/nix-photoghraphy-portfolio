export const metadata = {
  title: 'Sanity Studio | Nix Portfolio',
  description: 'Manage the cinematic content and photographic archives for Nix Portfolio.',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
